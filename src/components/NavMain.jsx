import React from 'react';
import '../App.css';
import NavOptions from './NavOptions'

const Navbar = () => {

    return (
        <nav className="navbar">
            <NavOptions/>
        </nav>
    );
}
export default Navbar;
